
# import os.path, pkgutil
# import doctest

# import pynsee.localdata
# pkgpath_localdata = os.path.dirname(pynsee.localdata.__file__)
# modules_localdata = [pkgpath_localdata + '\\'+ name + '.py' for _, name, _ in pkgutil.iter_modules([pkgpath_localdata])]

# doctest.testfile(modules_localdata[16])

# import pynsee.macrodata
# pkgpath_macrodata = os.path.dirname(pynsee.macrodata.__file__)
# modules_macrodata = [name for _, name, _ in pkgutil.iter_modules([pkgpath_macrodata])]

# import pynsee.metadata
# pkgpath_metadata = os.path.dirname(pynsee.metadata.__file__)
# modules_metadata = [name for _, name, _ in pkgutil.iter_modules([pkgpath_metadata])]

# import pynsee.sirene
# pkgpath_sirene = os.path.dirname(pynsee.sirene.__file__)
# modules_sirene = [name for _, name, _ in pkgutil.iter_modules([pkgpath_sirene])]
